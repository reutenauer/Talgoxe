aljuver.  2a1?

Headwords with parenthesis.

SO tunn-ös TIP \'huvudskål\'

IP är inte nödvändigtvis en punkt!

No longer bugs:
1. Better search interface
2. Create article
3. Type.objects.get(abbrev = 'GO') gets GÖ!
4. Proper Swedish sorting order
5. Offer possibility to edit entry word, endpoint for that action.
6. Delete word
7. Reset start of increment?
8. Log POST data?
