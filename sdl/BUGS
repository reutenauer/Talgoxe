aljuver.  2a1?

Headwords with parenthesis.

SO tunn-ös TIP \'huvudskål\'

IP är inte nödvändigtvis en punkt!

No longer bugs:
  * Better search interface
  * Type.objects.get(abbrev = 'GO') gets GÖ!
  * Proper Swedish sorting order
  * Delete word
  * Reset start of increment?
  * Add list of words to index.
